---

- name: install packages for python
  become: true
  apt: name="{{item}}" state=present
  with_items:
    - ipython
    - libmysqlclient-dev
    - python-dev
    - python-flake8
    - python-pip
    - python-pytest
    - python3-venv
    - virtualenv
